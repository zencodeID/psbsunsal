import 'package:flutter/material.dart';

class KegiatanSTIT extends StatelessWidget {
  const KegiatanSTIT({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Kegiatan Mahasiswa STIT",
        style: TextStyle(
          fontSize: 14.0,
        ),
      ),
    );
  }
}
